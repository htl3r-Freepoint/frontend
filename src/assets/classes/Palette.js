export default class Palette {
    constructor(main, text, background, banner) {
        this.main = main
        this.text = text
        this.background = background
        this.banner = banner
    }
}